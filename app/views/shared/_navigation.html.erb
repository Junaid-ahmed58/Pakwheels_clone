<div class="nav-bar-back">
  <nav class="navbar navbar-expand-lg ,bg-body-tertiary">
    <div class="container-fluid">
      <div class="nav-logo">
        <a href="/cars">
          <%= image_tag "admin-logo.png", class: "main-logo" %>
        </a>
      </div>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ">
          <%= link_to "My Cars", my_cars_cars_path, class: "btn btn-secondary margin" %>
          <%= link_to new_car_url, class: "btn btn-secondary " do %>
            <i class="fa-solid fa-car"></i> Post an ad
          <% end %>
          <div class="btn-margin">
            <% if current_user %>
              <%= button_to destroy_user_session_path,  method: :delete, class: "btn btn-danger" do %>
                <i class="fa-solid fa-right-from-bracket"></i>Logout
              <% end %>
            <% else %>
              <%= link_to "Sign up", new_user_registration_path, class: "btn btn-secondary" %>
              <%= link_to "Sign in", new_user_session_path, class: "btn btn-secondary" %>
            <% end %>
          </div>
        </ul>
      </div>
    </div>
  </nav>
</div>
